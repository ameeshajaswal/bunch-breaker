{
  "builds": [
    {
      "src": "backend/src",
      "use": "@railway/python"
    }
  ],
  "deploy": {
    "startCommand": "cd backend/src && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
  }
}